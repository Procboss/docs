# Google Analytics measurement ID (GA4), e.g. G-XXXXXXXXXX.
# In production this is set in Cloudflare Pages → Settings → Environment
# variables (Production). A rebuild is required — the ID is inlined into the
# static HTML at build time. Unset = no analytics snippet at all.
# PUBLIC_GA_ID=G-XXXXXXXXXX
